// Backwards compatibility - Re-export from new modular structure
// This follows the Open/Closed Principle - existing code doesn't need to change
export { GameRenderer as Renderer } from './renderers/GameRenderer.js';
